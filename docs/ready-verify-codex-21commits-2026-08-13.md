# READY: 코덱스 대증 수리 21커밋 검수 (read-only, Grok 4.5 high, 반대편 검증)

대상: /Users/yedulab/yupack 레포 main의 48d2f90..3861898 (21커밋, +1202/-238, 주 파일 yupack_mcp/server.py·local_pack.py). 코드 수정·커밋 금지. 산출 = /Users/yedulab/yupack/docs/verify-codex-21commits-verdict.md (첫 줄 PASS/FAIL/CONDITIONAL, 한국어).

## 배경 (오너 결함 신고 원문 요지, 2026-08-12 심야)
- 유팩으로 수업 중 팩이 자꾸 경량화 생성됨, 키네틱 없으면 답변 생성이 막힘.
- 팩 생성 자체가 실패, 품질 극히 저하. 폴더 경로 자동 탐색 실패, 플러그인 연결 없음 오류.
- 임베딩이 옛 모델(정본 = 로컬 OMLX bge-m3 1024d, 키 없음 체제).
- 오너 확정 방향: "유크라테스 코드를 그대로 가져오고 로컬 부분(DB 저장·임베딩)만 교체" + "질의는 벡터+키워드+그래프, 유크라테스와 동일".

코덱스는 위 신고에 대해 대증 수리 21커밋을 main에 쌓고, 근본 재작업 플랜(docs/superpowers/plans/2026-08-13-yucrates-local-authoring-adapter.md)을 작성한 뒤 리밋으로 중단했다. 이 검수는 "21커밋을 유지한 채 플랜 재작업을 얹어도 되는지"를 판정한다.

## 축
① **오너 신고 6건 해소 실측**: 경량화 팩 방지 / 키네틱 부재 시 grounded 답변 차단 해제(graph-optional) / 팩 생성 성공(keyless) / 폴더 자동탐색 금지+질문 / 플러그인 연결 / bge-m3 기본. 각각 대응 커밋을 지목하고, 실코드·실테스트로 해소 여부를 실측(주장 아닌 재현). 테스트 실행: uv run pytest 또는 python -m pytest (레포 관행 확인 후).
② **역행 결함**: 21커밋이 서로 되돌리거나(예: 후속 커밋이 선행 수정을 무효화) 기존 v0.3.x 동작(48d2f90의 grounding strong 판정 등)을 깨는 지점.
③ **플랜 정합**: 21커밋의 방향이 플랜 원칙 5(DB 드라이버 복사 금지·호스트 저작·키네틱 조건부·폴더 질문·정본 불변)와 충돌하는 지점 - 플랜 재작업 시 걷어내야 할 커밋 목록.
④ **위생**: 하드코딩 경로 잔존 0(rg로 /Users/yedulab 검색), 시크릿 노출 0, em dash 무관(영문 코드베이스).
⑤ **미커밋 잔재 판정**: 레포 미커밋 docs/·uv.lock 의 커밋 여부 권고.

## 게이트
- 전 판정은 실측 인용(파일:행, 테스트 출력)으로. 축 밖 관찰은 NOTE.
- 검수 중 어떤 파일도 수정 금지. 테스트 실행은 read-only 부산물(캐시)만 허용.
