# 과업: yupack 거버넌스 도구 18종 로컬 구현

## 오너 지시 원문 (verbatim 앵커)
> "workflow·approval·promotion·ReBAC은 cloud 서비스가 없어도 local policy와 audit log로 동일한 의미를 유지한다."
> "candidate_not_independently_reviewed를 reviewed로 자동 승격하지 않는다."
> "authoring, validation, independent review, promotion은 분리한다."
> "수정은 원본 ZIP을 덮어쓰지 않고 local overlay/fork와 immutable audit event로 남긴다."
> "billing_get_usage # 로컬 실행·인덱스·저장소 사용량으로 구현 / billing_list_events # 로컬 audit event로 구현"

## 컨텍스트
- 레포: 이 디렉터리 (yupack MCP 서버, FastMCP Python).
- `yupack_mcp/local_pack.py`: LocalPack 엔진 완성돼 있음. `local_pack.get(handle)`로
  열린 팩을 얻는다. `pk._runtime_db()`가 runtime/ontology.sqlite 연결을 주며 테이블
  overlay_nodes/overlay_edges/audit_events/workflow_runs/approvals/promotions/
  identity_aliases/duplicates가 이미 생성돼 있다. `pk._audit(action, detail)` 사용.
- `yupack_mcp/server.py`: `@mcp.tool()`로 도구 등록. 기존 스타일(한국어 docstring,
  dict 반환, error는 {"error": ...}) 그대로 따를 것. em dash(—) 절대 사용 금지.

## 구현할 파일
`yupack_mcp/governance.py` 신규 + `server.py` 끝부분(마지막 도구 뒤)에 18종 등록.
모든 도구의 첫 인자는 `pack_handle: str` (local_pack.get으로 해석, 없으면 error).

## 도구 18종과 로컬 의미
1. workflow_create_run(pack_handle, kind) -> run_id 생성, workflow_runs에 state="authoring"
2. workflow_advance(pack_handle, run_id) -> 단계 authoring->validation->independent_review->promotion 순서로만 전진. 건너뛰기 불가
3. approval_request(pack_handle, target, reason) -> approvals에 status="pending" 기록만 (자동 승인 없음)
4. harness_promotion_apply(pack_handle, candidate_id) -> promotions에서 status="promoted"인 것만 적용 표시, 아니면 error
5-9. identity_add_alias / identity_resolve_canonical / identity_propose_duplicate /
     identity_resolve_duplicate(a,b,decision: merge|distinct) / identity_list_pending_duplicates
     -> identity_aliases/duplicates 테이블 CRUD. resolve_canonical은 alias 체인 따라 canonical id 반환
10. canonicalize_merge_nodes(pack_handle, keep_id, drop_id) -> 원본 무수정. overlay_nodes에
    drop_id를 op="merged_into:keep_id"로 기록 + identity_aliases에 (drop->keep) 추가
11. canonicalize_find_and_propose(pack_handle, threshold=0.9) -> pk.nodes에서 label이
    정확히 같은(대소문자/공백 무시) 노드쌍을 찾아 duplicates에 status="pending" 등록, 목록 반환 (최대 20)
12-15. promotion_register_candidate(pack_handle, node_id) -> status="candidate_not_independently_reviewed"
       promotion_validate_candidate -> validation 필드에 검사 결과 기록 (evidence_refs 존재 여부 등),
       status는 바꾸지 않음
       promotion_promote(pack_handle, candidate_id, independent_review: bool=False) ->
       independent_review=True가 아니면 반드시 error("독립 검수 없이 승격 불가"). True면 status="promoted"
       promotion_reject -> status="rejected"
16. billing_get_usage(pack_handle) -> zip 크기, 캐시 디렉터리 크기, 노드/벡터 수, audit event 수
17. billing_list_events(pack_handle, limit=50) -> audit_events 최신순
18. ontology_rebac_check(pack_handle, actor, action) -> runtime/policy.yaml의 rebac 절 파싱
    (yaml 라이브러리 없이 단순 파싱 가능한 형식임), 허용 여부 반환. 모르는 actor는 read만

모든 쓰기 후 pk._audit(도구명, 상세) 호출. 원본 데이터 파일(nodes.jsonl 등) 절대 수정 금지.

## 검증 (필수, 커밋 전)
- `python3 -m py_compile yupack_mcp/governance.py yupack_mcp/server.py`
- 간단 스모크: python3 -c로 local_pack.open_local('<이 레포 밖 아무 queryable zip>')이 없으니,
  `python3 - <<'PY'` 스크립트로 zipfile로 최소 팩 zip(tmp)을 만들어 open_local -> workflow 생성 ->
  advance 4단계 -> promote(independent_review=False)가 error인지 -> True면 promoted인지 확인
- em dash 0: `grep -c '—' yupack_mcp/governance.py` = 0
- 완료 후 git add -A && git commit -m "거버넌스 도구 18종 로컬 구현 (policy+audit)" (push는 하지 말 것)
